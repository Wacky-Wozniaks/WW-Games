LATEST PUT INTO AWS: 5

In this folder will be files that contain the SQL updates to the database that you have done on your local database. Ultimately these changes will be put into the AWS database.

When you should do this:
 * When a table is added/dropped
 * Whenever SQL is run to change the format/layout of the database
 * Any other necessary time

The file name should always be in the format SchemaSetup187.sql
                                                        ^^^
                                        The next number (starting from 1)